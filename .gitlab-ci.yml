image: "ruby:2.7"

before_script:
  - apt-get update -qq && apt-get install -y nodejs npm
  - npm install yarn -g
  - gem install bundler --no-document
  - bundle install

all_tests:
  script:
    - bundle exec rspec
